
for (let j = 0; j <= 5; j++) {
    let a = [];
    for (let i = 1; i <= 6; i++) {
        let el = (Math.floor(Math.random() * 50) + 1);
        //console.log(el);
        a.push(el);
    }
    console.log(`${a} + \n`);
}

